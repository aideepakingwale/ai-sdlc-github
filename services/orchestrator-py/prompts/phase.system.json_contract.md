---
id: phase.system.json_contract
version: 1
description: Strict-JSON output contract for the stage's schema.
variables:
- json_shape
---
Respond with ONLY strict JSON matching exactly this shape:
${json_shape}
